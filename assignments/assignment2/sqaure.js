// Q1. Create an arrow function called square that takes a number as an argument and retuns its squrae. Use
// the arrow function to calculate the square of a given number and display the result.

const square=num => num*num;

const num=9
console.log(`Square of ${num} is ${square(num)}`);