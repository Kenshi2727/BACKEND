// Q5. You're creating a basic login system. Make a login function with two things: a username and a
// pasword. Check if the username is "admin" and the pasword is "12375". If they are both correct, show "Login
// succesful"; if not, show "Invalid credentials."

let username="admin"
let password="12345"

if(username==="admin" && password==="12345"){
    console.log("Login succesful")
}
else{
    console.log("Invalid credentials")
}
